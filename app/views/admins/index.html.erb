<h1>Admins Dashboard</h1>

<% @user.each do |u| %>
  <p>
    <%= u.first_name %> <%= u.last_name %> : <%= u.email %>
    <a class='btn btn-primary' href='<%= edit_admin_user_path(admin_id: current_user.id, id: u.id)%>'>éditer</a> 
    <%= link_to "supprimer", admin_user_path(admin_id: current_user.id, id: u.id), method: :delete, class: 'btn btn-danger' %>
  </p>
<% end %>

